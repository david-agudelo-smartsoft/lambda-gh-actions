name: Deploy Lambda Function

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v2

      - name: Configurar AWS CLI
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIA6L3TPAHHDOKWFYUV
          aws-secret-access-key: 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
          aws-region: us-east-1

      - name: Deploy
        run: |
          aws lambda invoke --function-name testlambda index.msj
          cat index.msj
          ls -la
          zip -j deploy.zip
          aws lambda update-function-code --function-name testlambda --zip-file fileb://deploy.zip
